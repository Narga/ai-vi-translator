# v4.0.0
# Các thư viện cần thiết cho dự án Novel Translator

# Gemini API SDKs
google-genai>=1.0.0              # SDK mới (khuyến nghị, mặc định)
google-generativeai>=0.8.0       # SDK cũ (fallback, legacy)

# Tiện ích
chardet
tqdm
psutil  # Cho Health Monitor memory tracking (optional)

# Các gói cần thiết cho Tiện ích chuyển đổi Văn bản sang EPUB

# Bắt buộc cho việc xử lý Markdown khi bật cờ --src-md
python-markdown

# Bắt buộc cho việc đóng gói file .epub
ebooklib

# Bắt buộc cho việc đọc và xử lý metadata Calibre
lxml

# Bắt buộc cho việc xử lý HTML
beautifulsoup4 

# Bắt buộc cho công cụ chuyển đổi epub2text
html2text

# OCR Plugin dependencies (v3.0.3)
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0
pdfplumber>=0.9.0
PyPDF2>=3.0.0
PyMuPDF>=1.23.0,<1.26.5
python-docx>=1.0.0
ocrmypdf>=15.0.0
PyYAML

# OCR Plugin optional dependencies (for advanced features)
# unstructured[local-inference]  # Advanced table extraction
# opencv-python  # Advanced table extraction